THE ASSIGNMENT OPERATOR

The assignment operator stores values.
The assignment operator is used for giving data to variables.
Example:
	int x;
	x = 10;


THE EQUALITY OPERATOR

The equality operator is used for checking if conditions.
The equality operator compares two values.
Example:
	int x = 10;
	if (x == 10) {
		cout << "x is 10";
	}
